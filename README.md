# Vega YouTube Provider

Install this repository in Vega by entering the full repository URL:

`https://github.com/suzainhaider124-spec/vega-youtube-extension`

The provider ID must match the `providers/youtube-provider` folder. Vega loads the bundled modules from `dist/youtube-provider/` after installation.
